% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_roc_curves}
\alias{plot_roc_curves}
\title{Plot overlaid ROC curves}
\usage{
plot_roc_curves(df, group_col, vars, min_cases = 10)
}
\arguments{
\item{df}{A data frame containing \code{group_col} and \code{var}.}

\item{group_col}{Name of the 0/1 outcome column.}

\item{vars}{Character vector of predictor names; only numeric columns are
used, and at least two are required.}

\item{min_cases}{Minimum number of complete cases required to fit; below this
the function returns \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} object, or \code{NULL} if no curve can be drawn.
}
\description{
Draws one ROC curve per numeric predictor (and a combined multivariable
curve when applicable) on a single set of axes, with AUCs shown in the
legend. Curves come from the same models as \code{\link[=compute_roc_table]{compute_roc_table()}}.
}
\examples{
set.seed(1)
df <- data.frame(grp = rep(c(0, 1), each = 40),
                 x = c(rnorm(40), rnorm(40, 1)),
                 z = c(rnorm(40), rnorm(40, 0.8)))
p <- plot_roc_curves(df, "grp", c("x", "z"))

}
