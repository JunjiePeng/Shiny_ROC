% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{compute_roc_table}
\alias{compute_roc_table}
\title{Compute a table of ROC / AUC results}
\usage{
compute_roc_table(df, group_col, vars, min_cases = 10)
}
\arguments{
\item{df}{A data frame containing \code{group_col} and \code{var}.}

\item{group_col}{Name of the 0/1 outcome column.}

\item{vars}{Character vector of predictor names; only numeric columns are
used, and at least two are required.}

\item{min_cases}{Minimum number of complete cases required to fit; below this
the function returns \code{NULL}.}
}
\value{
A tibble with one row per predictor (plus an optional \code{Combined}
row) and columns \code{model}, \code{predictors}, \code{auc}, \code{auc_ci_low},
\code{auc_ci_high}, \code{best_threshold}, \code{sensitivity}, \code{specificity}, \code{n},
\code{note}.
}
\description{
For each predictor, fits a univariate logistic ROC (via
\code{\link[=roc_from_predictor]{roc_from_predictor()}}) and reports AUC with confidence interval and the
Youden-optimal threshold, sensitivity and specificity. When two or more
numeric predictors are supplied, an additional combined multivariable model
is appended (via \code{\link[=roc_combined]{roc_combined()}}).
}
\examples{
set.seed(1)
df <- data.frame(grp = rep(c(0, 1), each = 40),
                 x = c(rnorm(40), rnorm(40, 1)),
                 z = c(rnorm(40), rnorm(40, 0.8)))
compute_roc_table(df, "grp", c("x", "z"))

}
