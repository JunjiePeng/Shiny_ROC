% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{roc_from_predictor}
\alias{roc_from_predictor}
\title{Fit a univariate logistic ROC curve}
\usage{
roc_from_predictor(df, group_col, var, min_cases = 10)
}
\arguments{
\item{df}{A data frame containing \code{group_col} and \code{var}.}

\item{group_col}{Name of the 0/1 outcome column.}

\item{var}{Name of a single numeric predictor column.}

\item{min_cases}{Minimum number of complete cases required to fit; below this
the function returns \code{NULL}.}
}
\value{
A \code{\link[pROC:roc]{pROC::roc}} object, or \code{NULL} if the predictor is non-numeric or
there are too few complete cases.
}
\description{
Fits a logistic regression of the 0/1 outcome on a single numeric predictor
and returns the ROC curve of the fitted probabilities. This is the shared
primitive used by both \code{\link[=compute_roc_table]{compute_roc_table()}} and \code{\link[=plot_roc_curves]{plot_roc_curves()}}, so the
curves in the plot and the numbers in the table always come from the same
model.
}
\examples{
set.seed(1)
df <- data.frame(grp = rep(c(0, 1), each = 30),
                 x = c(rnorm(30), rnorm(30, 1.5)))
r <- roc_from_predictor(df, "grp", "x")
if (!is.null(r)) as.numeric(pROC::auc(r))

}
