% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_group_boxplots}
\alias{plot_group_boxplots}
\title{Faceted group boxplots with p-value annotations}
\usage{
plot_group_boxplots(df, group_col, vars, p_tbl, use_adj = TRUE)
}
\arguments{
\item{df}{A data frame containing \code{group_col} and \code{vars}.}

\item{group_col}{Name of the 0/1 grouping column.}

\item{vars}{Character vector of numeric variables to plot.}

\item{p_tbl}{A p-value table as returned by \code{\link[=compute_univariate_tests]{compute_univariate_tests()}}.}

\item{use_adj}{Logical; annotate with FDR-adjusted p-values (\code{TRUE}) or raw
p-values (\code{FALSE}).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} object.
}
\description{
Draws a boxplot (with jittered points) of each variable split by the two
outcome groups, faceted by variable and annotated with the (optionally
FDR-adjusted) p-value from \code{\link[=compute_univariate_tests]{compute_univariate_tests()}}.
}
\examples{
set.seed(1)
df <- data.frame(grp = rep(c(0, 1), each = 20),
                 x = c(rnorm(20), rnorm(20, 1)))
pt <- compute_univariate_tests(df, "grp", "x", test = "t")
plot_group_boxplots(df, "grp", "x", pt)

}
