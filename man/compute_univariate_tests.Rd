% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{compute_univariate_tests}
\alias{compute_univariate_tests}
\title{Univariate group-comparison tests}
\usage{
compute_univariate_tests(
  df,
  group_col,
  vars,
  test = c("t", "wilcox"),
  adjust_fdr = TRUE
)
}
\arguments{
\item{df}{A data frame containing \code{group_col} and \code{vars}.}

\item{group_col}{Name of the 0/1 grouping column (see
\code{\link[=coerce_binary_group]{coerce_binary_group()}}).}

\item{vars}{Character vector of variable names to test.}

\item{test}{Either \code{"t"} (Welch t-test) or \code{"wilcox"} (Mann-Whitney).}

\item{adjust_fdr}{Logical; if \code{TRUE}, add an FDR-adjusted p-value column.}
}
\value{
A tibble with one row per variable and columns \code{variable}, \code{n0},
\code{n1}, \code{statistic}, \code{p_value}, \code{note}, and \code{p_adj_fdr}.
}
\description{
Runs a per-variable two-group comparison (Welch t-test or Mann-Whitney /
Wilcoxon rank-sum) between the two levels of a 0/1 grouping column, with
optional Benjamini-Hochberg (FDR) adjustment of the p-values.
}
\examples{
set.seed(1)
df <- data.frame(
  grp = rep(c(0, 1), each = 20),
  x = c(rnorm(20), rnorm(20, 1)),
  y = rnorm(40)
)
compute_univariate_tests(df, "grp", c("x", "y"), test = "t")

}
