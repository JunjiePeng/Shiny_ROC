% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io.R
\name{read_data}
\alias{read_data}
\title{Read a data file into a data frame}
\usage{
read_data(path, ext, sheet = NULL, delim = "\\t")
}
\arguments{
\item{path}{Path to the file to read.}

\item{ext}{File extension (without the dot), e.g. \code{"xlsx"}, \code{"csv"}, \code{"tsv"}.
Case-insensitive.}

\item{sheet}{For Excel files, the sheet name or index to read. If \code{NULL} or
\code{""}, the first sheet is read.}

\item{delim}{For \code{.txt}/\code{.tsv} files, the field delimiter. Defaults to a tab.}
}
\value{
A \link[tibble:tibble]{tibble} / data frame of the file contents.
}
\description{
Reads tabular data from Excel (\code{.xlsx}/\code{.xls}), comma-separated (\code{.csv}), or
delimited text (\code{.txt}/\code{.tsv}) files. This is the file-import engine used by
the Shiny app, exposed so the same logic can be reused programmatically.
}
\examples{
tmp <- tempfile(fileext = ".csv")
utils::write.csv(data.frame(a = 1:3, b = 4:6), tmp, row.names = FALSE)
read_data(tmp, ext = "csv")

}
